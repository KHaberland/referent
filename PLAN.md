# План добавления AI-функций

На основании PROJECT.md необходимо реализовать функциональность для трёх кнопок, которые используют AI для анализа англоязычных статей.

---

## 1. Кнопка «О чем статья?»

### Цель
Генерация краткого описания статьи на русском языке — о чём она, какова главная идея.

### Порядок действий

1. **Создать API route** `app/api/summary/route.ts`
   - Принимает: `content` (текст статьи), `title` (заголовок)
   - Использует OpenRouter API с моделью DeepSeek
   - Системный промпт: «Ты — аналитик. Прочитай англоязычную статью и кратко опиши на русском языке, о чём она. Выдели главную идею и ключевые моменты. Ответ должен быть 3-5 предложений.»

2. **Обновить `page.tsx`**
   - В блоке `else` функции `handleAction` добавить обработку `action === 'summary'`
   - Вызвать `/api/summary` с контентом статьи
   - Вывести результат в поле «Результат»

---

## 2. Кнопка «Тезисы»

### Цель
Генерация списка ключевых тезисов статьи на русском языке.

### Порядок действий

1. **Создать API route** `app/api/theses/route.ts`
   - Принимает: `content` (текст статьи), `title` (заголовок)
   - Использует OpenRouter API с моделью DeepSeek
   - Системный промпт: «Ты — аналитик. Прочитай англоязычную статью и выдели 5-7 ключевых тезисов на русском языке. Каждый тезис должен быть кратким (1-2 предложения). Формат: нумерованный список.»

2. **Обновить `page.tsx`**
   - Добавить обработку `action === 'theses'`
   - Вызвать `/api/theses` с контентом статьи
   - Вывести результат в поле «Результат»

---

## 3. Кнопка «Пост для Telegram»

### Цель
Генерация готового поста для Telegram-канала на русском языке с кратким пересказом статьи.

### Порядок действий

1. **Создать API route** `app/api/telegram/route.ts`
   - Принимает: `content` (текст статьи), `title` (заголовок), `url` (ссылка на оригинал)
   - Использует OpenRouter API с моделью DeepSeek
   - Системный промпт: «Ты — SMM-специалист. Напиши пост для Telegram-канала на русском языке по мотивам англоязычной статьи. Пост должен быть информативным, легко читаемым, 150-300 слов. Используй эмодзи умеренно. В конце добавь ссылку на оригинал.»

2. **Обновить `page.tsx`**
   - Добавить обработку `action === 'telegram'`
   - Вызвать `/api/telegram` с контентом статьи и URL
   - Вывести результат в поле «Результат»

---

## Общая структура API routes

Все три API route будут иметь одинаковую структуру (аналогично `/api/translate`):

```
app/api/
├── parse/
│   └── route.ts       ✅ (уже есть)
├── translate/
│   └── route.ts       ✅ (уже есть)
├── summary/
│   └── route.ts       📝 (создать)
├── theses/
│   └── route.ts       📝 (создать)
└── telegram/
    └── route.ts       📝 (создать)
```

---

## Альтернативный вариант: Единый API route

Вместо трёх отдельных route можно создать один универсальный `app/api/ai/route.ts`, который принимает параметр `action` и выбирает соответствующий промпт.

### Преимущества:
- Меньше дублирования кода
- Проще поддерживать

### Недостатки:
- Сложнее тестировать отдельные функции
- Менее гибко для будущих изменений

---

## Порядок реализации

1. ✅ Перевод (уже реализован)
2. 📝 «О чем статья?» — создать `/api/summary`
3. 📝 «Тезисы» — создать `/api/theses`
4. 📝 «Пост для Telegram» — создать `/api/telegram`
5. 📝 Обновить `page.tsx` — интегрировать вызовы всех API
6. 📝 Тестирование на локальном сервере
7. 📝 Деплой на Vercel

---

## Используемые технологии

- **API:** OpenRouter (https://openrouter.ai/api)
- **Модель:** DeepSeek (`deepseek/deepseek-chat`)
- **API-ключ:** из переменной окружения `OPENROUTER_API_KEY`

